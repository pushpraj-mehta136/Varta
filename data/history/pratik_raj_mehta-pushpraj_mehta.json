[
  {
    "from": "pratik_raj_mehta",
    "to": "pushpraj_mehta",
    "message": "const fs = require(\"fs\"); const path = require(\"path\");  const HISTORY_DIR = path.join(__dirname, \"../data/history\"); const USERS_DIR = path.join(__dirname, \"../data/users\");  fs.mkdirSync(HISTORY_DIR, { recursive: true }); fs.mkdirSync(USERS_DIR, { recursive: true });  // === Helper: Get consistent file path === function getChatFile(from, to) {   return path.join(HISTORY_DIR, `${[from, to].sort().join(\"-\")}.json`); }  // === Save a message === function saveMessage(from, to, msg) {   const file = getChatFile(from, to);   const history = fs.existsSync(file) ? JSON.parse(fs.readFileSync(file)) : [];   history.push(msg);   fs.writeFileSync(file, JSON.stringify(history, null, 2)); }  // === Update status of a message === function updateMessageStatus(from, to, timestamp, status) {   const file = getChatFile(from, to);   if (!fs.existsSync(file)) return;    const history = JSON.parse(fs.readFileSync(file));   const msg = history.find(m => m.timestamp === timestamp && m.from === from);   if (msg) {     msg.status = status;     fs.writeFileSync(file, JSON.stringify(history, null, 2));   } }  // === Update message reaction === function updateMessageReaction(from, to, timestamp, username, emoji) {   const file = getChatFile(from, to);   if (!fs.existsSync(file)) return;    const history = JSON.parse(fs.readFileSync(file));   const msg = history.find(m => m.timestamp === timestamp);   if (!msg) return;    msg.reactions = msg.reactions || {};    if (emoji) {     msg.reactions[username] = emoji;   } else {     delete msg.reactions[username];   }    fs.writeFileSync(file, JSON.stringify(history, null, 2)); }  // === Last seen tracking === function updateLastSeen(username, iso) {   const userFile = path.join(USERS_DIR, `${username}.json`);   const data = fs.existsSync(userFile) ? JSON.parse(fs.readFileSync(userFile)) : {};   data.lastSeen = iso;   fs.writeFileSync(userFile, JSON.stringify(data, null, 2)); }  function getLastSeen(username) {   const userFile = path.join(USERS_DIR, `${username}.json`);   if (!fs.existsSync(userFile)) return \"Offline\";   const data = JSON.parse(fs.readFileSync(userFile));   return data.lastSeen || \"Offline\"; }  // === In-memory state === const sockets = {}; const users = {}; const lastSeen = {};  function emitTo(username, event, data, io) {   const socketId = sockets[username];   if (socketId) {     io.to(socketId).emit(event, data);   } }  // === Init Sockets === function initSockets(io) {   io.on(\"connection\", (socket) => {      // === User joins ===     socket.on(\"join\", (username) => {       sockets[username] = socket.id;       users[socket.id] = username;       lastSeen[username] = \"Online\";       io.emit(\"active-users\", Object.keys(sockets));        // Deliver any \"sent\" messages to the now-online user       const files = fs.readdirSync(HISTORY_DIR);       files.forEach(file => {         const filePath = path.join(HISTORY_DIR, file);         const history = JSON.parse(fs.readFileSync(filePath));         let updated = false;          history.forEach(msg => {           if (msg.to === username && msg.status === \"sent\") {             msg.status = \"delivered\";             emitTo(msg.to, \"receive-message\", msg, io);             emitTo(msg.from, \"message-status-update\", {               timestamp: msg.timestamp,               status: \"delivered\"             }, io);             updated = true;           }         });          if (updated) {           fs.writeFileSync(filePath, JSON.stringify(history, null, 2));         }       });     });      // === Message sent ===     socket.on(\"private-message\", ({ to, message, timestamp, type, originalName }) => {       const from = users[socket.id];       const msg = {         from,         to,         message,         timestamp,         type,         originalName,         status: \"sent\",         reactions: {}       };        saveMessage(from, to, msg);        if (sockets[to]) {         msg.status = \"delivered\";         updateMessageStatus(from, to, timestamp, \"delivered\");          emitTo(to, \"receive-message\", msg, io);         emitTo(from, \"message-status-update\", { timestamp, status: \"delivered\" }, io);       } else {         emitTo(from, \"message-status-update\", { timestamp, status: \"sent\" }, io);       }     });      // === Reaction added/removed ===     socket.on(\"react-message\", ({ from, to, timestamp, emoji }) => {       const username = users[socket.id];       if (!from || !to || !timestamp || !username) return;        updateMessageReaction(from, to, timestamp, username, emoji);        emitTo(from, \"reaction-update\", { timestamp, username, emoji }, io);       emitTo(to, \"reaction-update\", { timestamp, username, emoji }, io);     });      // === Typing indicator ===     socket.on(\"typing\", ({ to }) => {       const from = users[socket.id];       if (from && to) {         emitTo(to, \"show-typing\", { from }, io);       }     });      // === Seen message ===     socket.on(\"seen-messages\", ({ from, to }) => {       const file = getChatFile(from, to);       if (!fs.existsSync(file)) return;        const history = JSON.parse(fs.readFileSync(file));       const lastMessage = [...history].reverse().find(m => m.from === from && m.to === to);       if (!lastMessage) return;        updateMessageStatus(from, to, lastMessage.timestamp, \"seen\");        emitTo(from, \"mark-seen\", { sender: to, timestamp: lastMessage.timestamp }, io);       emitTo(from, \"message-status-update\", {         timestamp: lastMessage.timestamp,         status: \"seen\"       }, io);     });      // === Request last seen ===     socket.on(\"get-last-seen\", (username) => {       const status = lastSeen[username] === \"Online\" ? \"Online\" : getLastSeen(username);       socket.emit(\"last-seen\", { username, lastSeen: status });     });      // === Cross-tab sync ===     socket.on(\"refresh-tabs\", ({ type, target }) => {       emitTo(target, \"refresh-data\", { type }, io);     });      // === On Disconnect ===     socket.on(\"disconnect\", () => {       const username = users[socket.id];       if (username) {         const iso = new Date().toISOString();         updateLastSeen(username, iso);         lastSeen[username] = iso;         delete sockets[username];         delete users[socket.id];         io.emit(\"active-users\", Object.keys(sockets));       }     });   }); }  module.exports = {   initSockets,   emitTo,   getSocketId: (username) => sockets[username] };",
    "timestamp": "2025-07-05T11:48:12.952Z",
    "type": "text",
    "status": "seen",
    "reactions": {
      "pushpraj_mehta": "❤️"
    }
  },
  {
    "from": "pushpraj_mehta",
    "to": "pratik_raj_mehta",
    "message": "Hlo",
    "timestamp": "2025-07-05T12:05:55.039Z",
    "type": "text",
    "status": "seen",
    "reactions": {
      "pushpraj_mehta": "❤️"
    }
  },
  {
    "from": "pratik_raj_mehta",
    "to": "pushpraj_mehta",
    "message": "/uploads/1751717528579_IMG_20250528_065319.jpg",
    "timestamp": "2025-07-05T12:12:11.931Z",
    "type": "file",
    "originalName": "IMG_20250528_065319.jpg",
    "status": "seen",
    "reactions": {
      "pratik_raj_mehta": "❤️"
    }
  }
]